import { SchedulerTable } from './SchedulerTable.js';

// Since the SchedulerTable is defined as a custom element, no additional code is needed herecustomElements.define('scheduler-table', SchedulerTable);